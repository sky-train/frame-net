const assert = require('assert');


const {
  getHeader,
  getBody,
  create,
  newKey,
  newHeader,
  pack,
  unpack,
  getType,
  toBuffer
} = require('../lib/parser');

describe('parser',()=>{

  it('to buffer', ()=>{
    assert
  })


  it('new heaser',()=>{
    const heaser = newHeader({
      type: 2,
    })
  })
})